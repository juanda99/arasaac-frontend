import { defineMessages } from "react-intl";

export default defineMessages({
  forbidden: {
    id: "permissionsErrorView.forbidden",
    description: "Error message",
    defaultMessage: "Your user role can't access this resource.",
  },
});
