import styled from 'styled-components'

const Logo = styled.img`
  display: block;
  margin: 0 auto;
  position: relative;
  width: 300px;
  // if not, it seemed not to be centered
  left: -50px;
`
export default Logo
