/*
 * Gallery Messages
 *
 * This contains all the text for the Gallery component.
 */
import { defineMessages } from "react-intl";

export default defineMessages({
  header: {
    id: "app.components.Gallery.header",
    defaultMessage: "This is the Gallery component !",
  },
});
