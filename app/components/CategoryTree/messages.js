import { defineMessages } from 'react-intl'

const scope = 'app.containers/categoryTree'
export default defineMessages({
  chooseSubcategory: {
    id: `${scope}.chooseSubcategory`,
    defaultMessage: 'Choose subcategory:',
  },
})
