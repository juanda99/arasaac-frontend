export default {
  appBar: {
    // position: 'fixed',
    // Needed to overlap the examples
    // zIndex: this.state.muiTheme.zIndex.appBar + 1,
    top: 0,
    width: "100%",
    overflow: "hidden",
    zIndex: 8,
  },
};
