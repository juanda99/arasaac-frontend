import React from 'react'
import MuiThemeProvider from 'material-ui/styles/MuiThemeProvider'
import { setFilterItems } from 'containers/MaterialsView/actions'
// import createComponentWithIntl from 'utils/createComponentWithIntl'
import { intlEnzymeComponent } from 'utils/createComponentWithIntl'
import Component from '../Size'


// jest.mock('material-ui/internal/Tooltip')

describe('<Size />', () => {
  /* it('renders correctly', () => {
    const tree = createComponentWithIntl(
      <MuiThemeProvider>
        <Component setFilterItems={setFilterItems} values={1} />
      </MuiThemeProvider>).toJSON()
    expect(tree).toMatchSnapshot()
  }) */
  /*
  it('should render the Filters configuration', () => {
    const renderedComponent = intlEnzymeComponent(
      <MuiThemeProvider>
        <Component setFilterItems={setFilterItems} values={1} />
      </MuiThemeProvider>
    )
    console.log(renderedComponent.debug())
    // expect(renderedComponent.find('Connect(Toggle)').length).toEqual(7)
  })
  */
})
